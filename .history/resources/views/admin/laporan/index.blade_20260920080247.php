@extends('layouts.backend')
@section('title', 'Laporan')
@section('content')<h2>Laporan</h2><form method="get"><label>Mulai <input type="date" name="tanggal_mulai" value="{{ $start }}"></label><label>Selesai <input type="date" name="tanggal_selesai" value="{{ $end }}"></label><label>Anggota <select name="anggota"><option value="">Semua anggota</option>@foreach($anggotaList as $item)<option value="{{ $item->id }}" @selected((string) $anggota === (string) $item->id)>{{ $item->nama_lengkap }}</option>@endforeach</select></label><button type="submit">Filter</button></form><h3>Ringkasan</h3><dl><dt>Jadwal</dt><dd>{{ $rekapJadwal->count() }}</dd><dt>Tugas</dt><dd>{{ $rekapTugas->count() }}</dd><dt>Presensi</dt><dd>{{ $rekapPresensi->count() }}</dd><dt>Upah</dt><dd>{{ $rekapUpah->count() }}</dd></dl><h3>Detail tugas</h3><table><tr><th>Kegiatan</th><th>Anggota</th><th>Status</th></tr>@foreach($rekapTugas as $item)<tr><td>{{ $item->jadwal->nama_kegiatan }}</td><td>{{ $item->anggota->nama_lengkap }}</td><td>{{ $item->status }}</td></tr>@endforeach</table>@endsection
