@extends('layouts.backend')
@section('title', 'Edit Upah')
@section('content')<h2>Edit Upah</h2><form method="post" action="{{ route('admin.upah.update', $upah) }}">@csrf @method('PUT')<label>Mode perhitungan <input name="mode_perhitungan" value="{{ old('mode_perhitungan', $upah->mode_perhitungan) }}" required></label><label>Tarif per satuan <input type="number" name="tarif_per_satuan" value="{{ old('tarif_per_satuan', $upah->tarif_per_satuan) }}" required></label><label>Jumlah satuan <input type="number" step="0.01" name="jumlah_satuan" value="{{ old('jumlah_satuan', $upah->jumlah_satuan) }}" required></label><label>Jumlah upah <input type="number" name="jumlah_upah" value="{{ old('jumlah_upah', $upah->jumlah_upah) }}" required></label><label>Status <select name="status_pembayaran"><option value="belum_dibayar">Belum dibayar</option><option value="sudah_dibayar" @selected($upah->status_pembayaran === 'sudah_dibayar')>Sudah dibayar</option></select></label><label>Metode pembayaran <input name="metode_pembayaran" placeholder="Transfer, tunai, e-wallet" value="{{ old('metode_pembayaran', $upah->metode_pembayaran) }}"></label><label>Tanggal pembayaran <input type="date" name="tanggal_pembayaran" value="{{ $upah->tanggal_pembayaran?->format('Y-m-d') }}"></label><label>Catatan <textarea name="catatan">{{ $upah->catatan }}</textarea></label><button type="submit">Simpan</button></form>@endsection
